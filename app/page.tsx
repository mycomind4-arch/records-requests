import Link from 'next/link'

export default function Home(){return <main className="landing"><header className="landingNav"><strong>My-CoMind <span>/ Records Requests</span></strong><nav><a href="#how">How it works</a><a href="#audit">Production audit</a><Link href="/dashboard">Open workspace →</Link></nav></header><section className="hero"><div className="eyebrow">PUBLIC RECORDS • GOVERNMENT RECORDS • FOIA</div><h1>Request the records you need. <em>Know what you actually received.</em></h1><p className="lede">Build precise public-records requests from plain English, track the agency response, organize the production, and audit what was produced against what you asked for.</p><div className="cta"><Link className="primary" href="/dashboard">Start a records request →</Link><a className="secondary" href="#how">See how it works</a></div><div className="trust">Evidence-first · Deadline-aware · Source-linked · Built for serious requests</div></section><section id="how" className="section"><div className="eyebrow">FROM IDEA TO RECORD SET</div><h2>One workspace for the entire request.</h2><div className="steps">{[['01','Describe','Tell us what you are trying to find in plain English.'],['02','Build','Turn the objective into precise record categories, dates, custodians, identifiers, formats, and exclusions.'],['03','Track','Preserve acknowledgements, extensions, fees, productions, and every communication on one timeline.'],['04','Audit','Compare the request against the production and surface evidence-backed gaps.']].map(([n,t,d])=><article key={n}><b>{n}</b><h3>{t}</h3><p>{d}</p></article>)}</div></section><section id="audit" className="dark"><div><div className="eyebrow">THE DIFFERENCE</div><h2>Most tools stop when the agency sends files. We start the audit.</h2><p>When you receive a production, the important question is not simply how many documents arrived. It is whether the production addresses the request.</p></div><div className="auditGrid">{['Unanswered request categories','Referenced attachments not produced','Missing or duplicate pages','Contradictory records','Unexplained redactions','Search or custodian gaps'].map(x=><div key={x}>✓ {x}</div>)}</div></section><section className="section"><div className="eyebrow">USE IT FOR</div><h2>Requests that matter.</h2><div className="chips">{['Police records','Code enforcement records','Property records','Permit records','Government emails','Planning records','Inspection records','Agency correspondence'].map(x=><span key={x}>{x}</span>)}</div><p className="fine">Public-records laws and deadlines vary by jurisdiction. The workspace identifies applicable policy sources when available and labels uncertainty instead of presenting assumptions as law.</p></section><section className="final"><h2>Start with one sentence.</h2><p>“I want everything the county has about this property from 2023 through 2026.”</p><Link className="primary" href="/dashboard">Build the request →</Link></section></main>}
