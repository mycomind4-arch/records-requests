import { CodeEnforcementBuilder } from './builder'

export const metadata = {
  title: 'Build a Code Enforcement Records Request | Records Requests',
  description: 'Build a targeted code enforcement records request for a property, parcel, violation, complaint, or enforcement case.',
}

export default function CodeEnforcementBuilderPage() {
  return <CodeEnforcementBuilder />
}
